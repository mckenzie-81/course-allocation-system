-- Create database for course allocation application
CREATE DATABASE course_allocation;

-- Connect to the database (run this separately or use \c in psql)
-- \c course_allocation

-- Grant privileges (optional, adjust as needed)
-- GRANT ALL PRIVILEGES ON DATABASE course_allocation TO postgres;

